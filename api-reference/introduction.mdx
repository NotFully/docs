---
title: 'Начало'
description: 'Общая информация и параметры авторизации для API'
---

<Note>
  Этот раздел содержит вводную информацию и описание методов авторизации для работы с API.
</Note>

## Добро пожаловать

Все конечные точки API защищены и требуют авторизации. Поддерживаются два способа:

### 🔐 API-ключ (APIKeyHeader)

Авторизация через заголовок `API-Access-Key`.

```http
GET /project HTTP/1.1
Host: api.spellit.ai
API-Access-Key: your-api-key-here
```

**Name:** `API-Access-Key`  
**In:** `header`

---

### 🧑‍💻 OAuth2 (password flow)

Используется стандартный `password` поток OAuth2 для получения токена.

**Token URL:** `/api/auth/login`  
**Flow:** `password`

#### Пример тела запроса

```json
{
  "username": "example@example.com",
  "password": "••••••••"
}
```

После получения токена вы должны передавать его в заголовке `Authorization`:

```http
Authorization: Bearer your_token_here
```

<Callout emoji="🔒">
Для большинства запросов необходимо наличие валидного токена или API-ключа.
</Callout>


### ID проекта

Проекты для загрузки команда Spellit создает сама, для получения обратитесь к команде Spellit и мы выдадим Вам ID вашего проекта