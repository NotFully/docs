---
title: 'Начало'
description: 'Общая информация и параметры авторизации для API'
---

<Note>
  Этот раздел содержит вводную информацию и описание методов авторизации для работы с API.
</Note>

## Добро пожаловать

<p>Для регистрации в нашей системе обратитесь к команде Spellit. Вы получите API ключ и ID проекта. Проект – это набор правил и критериев для анализа ваших звонков. Проект создается и настраивается специально под вас.</p>
<p>Чтобы отправить звонок на анализ, необходимо загрузить его в виде набора данных (Item-Set) и отправить в ваш проект, указав его ID.</p>
Все конечные точки API защищены и требуют авторизации.
Поддерживаются два способа:

### 🔐 API-ключ (APIKeyHeader)

Авторизация через API ключ (заголовок `API-Access-Key`).

```http
GET /project HTTP/1.1
Host: api.spellit.ai
API-Access-Key: your-api-key-here
```

**Name:** `API-Access-Key`  
**In:** `header`

---

### 🧑‍💻 OAuth2 (password flow)

Используется стандартный `password` поток OAuth2 для получения токена.

**Token URL:** `/api/auth/login`  
**Flow:** `password`

#### Пример тела запроса

```json
{
  "username": "example@example.com",
  "password": "••••••••"
}
```

После получения токена вы должны передавать его в заголовке `Authorization`:

```http
Authorization: Bearer your_token_here
```

<Callout emoji="🔒">
Для большинства запросов необходимо наличие валидного токена или API-ключа.
</Callout>

## Начало работы

  <p> Чтобы отправить звонок на анализ, необходимо сперва создать Item-Set, получить в ответ его ID. После чего загрузить в этот Item-Set файл звонка (Item).</p>
  <p> В данной документации представлены способы обращения по API к системе анализа звонков Spellit, указаны примеры таких запросов. Если у вас остались вопросы, свяжитесь с командой Spellit. </p>