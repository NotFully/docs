---
title: 'Получение CRM данных по ID item_set'
openapi: 'GET /client/item-set/{item_set_id}/crm-data'
---

<Callout emoji="📊">Возвращает CRM данные, связанные с указанным item_set.</Callout>

## Параметры

- `item_set_id` (int, path, **обязательный**) - ID item_set

## Примеры запросов

### Пример 1: Базовый запрос CRM данных

```bash
curl -X GET "https://api.spellit.ai/api/client/item-set/123/crm-data" \
  -H "API-Access-Key: your access key"
```

### Пример 2: Запрос с дополнительными заголовками

```bash
curl -X GET "https://api.spellit.ai/api/client/item-set/123/crm-data" \
  -H "API-Access-Key: your access key" \
  -H "Accept: application/json" \
  -H "Content-Type: application/json"
```

## Возможные ошибки

- `401 Unauthorized` - Отсутствует или неверный токен аутентификации
- `403 Forbidden` - Item_set не принадлежит текущему пользователю
- `404 Not Found` - Item_set с указанным ID не найден
- `500 Internal Server Error` - Внутренняя ошибка сервера

## Описание полей ответа

- `id_item_set` - ID item_set, для которого запрашивались CRM данные
- `crm_data` - объект с CRM данными или `null`, если данные отсутствуют
  - Структура CRM данных зависит от настроек клиента
  - Может содержать любые поля, переданные при создании item_set
- `created_at` - дата и время создания метаданных с CRM данными или `null`

## Примечания

- CRM данные извлекаются из `processing_parameters.extra_processing_data.crm_data`
- Если CRM данные не были переданы при создании item_set, поле `crm_data` будет `null`
- CRM данные могут быть любыми JSON объектами, структура определяется клиентом
